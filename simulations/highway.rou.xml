<routes>
    <!-- vtypeauto is required by PlatoonsTrafficManager -->
    <vType id="vtypeauto"
           accel="2.5"
           decel="9.0"
           sigma="0"
           length="4"
           minGap="2.5"
           maxSpeed="42"
           color="1,0,0"
           tau="0.1"
           speedFactor="1.0"/>

    <route id="highway" edges="highway"/>

    <!-- Do NOT add a static <flow> here.
         PlatoonsTrafficManager inserts vehicles dynamically via TraCI. -->
</routes>
